name: Listen Trigger 2
on:
  workflow_dispatch:
    inputs:
      pr_commit_sha:
        description: "Commit Id"
        required: true

jobs:
  listener-work-2:
    runs-on: ubuntu-latest
    timeout-minutes: 2

    steps:
    - name: Dispatch Build Success
      run: |
        echo "Yes2"